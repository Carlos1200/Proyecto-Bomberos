import React from 'react'
import { Modal } from '../Modal'

export const UsuarioModal = ({handleClose}) => {
    return (
        <Modal handleClose={handleClose}>
            <p>Hola</p>
        </Modal>
    )
}
